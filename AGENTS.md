# actioncable-go

Run `make check` before pushing code changes.

## Releases

Before preparing, tagging, publishing, or recovering a release, read
[RELEASING.md](RELEASING.md) completely and follow it. A published Go module tag is
immutable; recovery uses the existing tag or a new version, never a moved tag.
