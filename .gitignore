# Each language directory ignores its own build output. These are the root's.

# Go test and coverage output
*.out
*.test

# SwiftPM's output for the root distribution manifest
/.build/

# Editor and OS noise
.DS_Store
*.swp

# Worktrees
.claude/worktrees
